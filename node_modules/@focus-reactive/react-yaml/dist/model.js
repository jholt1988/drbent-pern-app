"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getCurrentWord = void 0;

var getCurrentWord = function getCurrentWord(text, pos) {
  var firstPart = text.slice(0, pos).split(/\s|\n/);
  var lastPart = text.slice(pos).split(/\s|\n/);
  var firstPiece = firstPart[firstPart.length - 1];
  var lastPiece = lastPart[0];
  var from = pos - firstPiece.length;
  var to = pos + lastPiece.length;
  return {
    word: "".concat(firstPiece).concat(lastPiece),
    from: from,
    to: to
  };
};

exports.getCurrentWord = getCurrentWord;